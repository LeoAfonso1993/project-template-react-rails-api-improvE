//import { useContext } from 'react';
//import { UserContext } from '../../contexts/UserContext';

function DashTraining(){

    // const {user} = useContext(UserContext);

    // const welcome = `Welcome ${user.name}`

    return "hi1"
}

export default DashTraining;